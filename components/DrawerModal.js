import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Modal, Text, Alert, View } from 'react-native';

export default function HomeDrawer() {
  
  return (
   
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});